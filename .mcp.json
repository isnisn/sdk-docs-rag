{
  "mcpServers": {
    "sdk-docs": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}/server", "server.py"],
      "env": {
        "SDK_DOCS_CONFIG": "${PWD}/sdk-docs.json"
      }
    }
  }
}
